import { RedisStreamsMonitor } from '@/components/redis-streams-monitor';

export default function RedisStreamsPage() {
  return (
    <div className="container mx-auto p-6">
      <RedisStreamsMonitor />
    </div>
  );
}
